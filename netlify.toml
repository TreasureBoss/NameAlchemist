[build]
command = "npm install && npm run build && pip install -r netlify/functions/requirements.txt -t netlify/functions/python"
publish = "dist"

[[redirects]]
from = "/api/generate-name"
to = "/.netlify/functions/generate-name"
status = 200